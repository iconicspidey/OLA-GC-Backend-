const student = require('./student');
const admin = require('./admin');
const question = require('./question');
const timer = require('./timer');
const exams = require('./exam');

module.exports = {
  student, admin, question, timer, exams
};
